<div id="reports">
  <%- [:notice, :warn, :error].each do |level| %>
    <%- if reports[level] && !reports[level].empty? %>
      <ul class="<%= level %>">
        <li><%= reports[level] %></li>
      </ul>
    <% end %>  
  <% end %>
</div>